<!DOCTYPE html>

<html class="dark" lang="fr"><head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Gestion des Classes - School RMS</title>
<!-- Google Fonts -->
<link href="https://fonts.googleapis.com" rel="preconnect"/>
<link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;700;900&amp;family=Spline+Sans:wght@300;400;500;600;700&amp;display=swap" rel="stylesheet"/>
<!-- Material Symbols -->
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet"/>
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet"/>
<!-- Tailwind CSS -->
<script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
<!-- Tailwind Configuration -->
<script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#38e07b",
                        "primary-dark": "#2ec268",
                        "background-light": "#f6f8f7",
                        "background-dark": "#111714",
                        "surface-dark": "#1d2621",
                        "surface-light": "#ffffff",
                        "border-dark": "#29382f",
                    },
                    fontFamily: {
                        "display": ["Spline Sans", "Noto Sans", "sans-serif"],
                        "body": ["Noto Sans", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "1rem",
                        "lg": "1.5rem",
                        "xl": "2rem",
                        "2xl": "3rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
<style>
        body {
            font-family: "Spline Sans", "Noto Sans", sans-serif;
        }
        /* Custom scrollbar for dark theme */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #111714; 
        }
        ::-webkit-scrollbar-thumb {
            background: #29382f; 
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #38e07b; 
        }
    </style>
</head>
<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-white h-screen overflow-hidden flex selection:bg-primary selection:text-black">
<!-- Sidebar Navigation -->
<aside class="hidden lg:flex flex-col w-72 h-full border-r border-border-dark dark:border-border-dark bg-background-light dark:bg-background-dark p-6">
<!-- Logo Area -->
<div class="flex items-center gap-3 mb-10 px-2">
<div class="size-10 bg-primary/20 rounded-xl flex items-center justify-center text-primary">
<span class="material-symbols-outlined">school</span>
</div>
<h2 class="text-xl font-bold tracking-tight">School RMS</h2>
</div>
<!-- Navigation Links -->
<nav class="flex-1 flex flex-col gap-2">
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="{{path('enseignant_dashboard')}}">
<span class="material-symbols-outlined filled">dashboard</span>
<span class="text-sm font-bold">Dashboard</span>
</a>
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="{{ path('enseignant_gestion_classes') }}">
<span class="material-symbols-outlined">class</span>
<span class="text-sm font-medium">Classes</span>
</a>
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="{{path('enseignant_gestion_etudiants')}}">
<span class="material-symbols-outlined">groups</span>
<span class="text-sm font-medium">Étudiants</span>
</a>
<div class="mt-auto">
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="#">
<span class="material-symbols-outlined">settings</span>
<span class="text-sm font-medium">Paramètres</span>
</a>
</div>
</nav>
</aside>
<!-- Main Content Wrapper -->
<main class="flex-1 flex flex-col h-full overflow-hidden relative">
<!-- Top Navigation Bar -->
<header class="flex items-center justify-between px-6 py-4 border-b border-border-dark dark:bg-background-dark z-20">
<!-- Mobile Menu Toggle (Visible only on small screens) -->
<button class="lg:hidden p-2 text-white">
<span class="material-symbols-outlined">menu</span>
</button>
<!-- Search Bar -->
<div class="hidden md:flex flex-1 max-w-lg">
<label class="flex w-full items-center gap-3 bg-surface-dark rounded-full px-4 py-2.5 border border-transparent focus-within:border-primary/50 transition-all">
<span class="material-symbols-outlined text-gray-400">search</span>
<input class="bg-transparent border-none text-white placeholder-gray-500 focus:ring-0 w-full text-sm" placeholder="Rechercher un élève, une classe..." type="text"/>
</label>
</div>
<!-- Right Actions -->
<div class="flex items-center gap-4 ml-auto">
<button class="relative p-2.5 rounded-full bg-surface-dark hover:bg-white/10 text-white transition-colors">
<span class="material-symbols-outlined text-[20px]">notifications</span>
<span class="absolute top-2 right-2.5 size-2 bg-primary rounded-full"></span>
</button>
<div class="flex items-center gap-3 pl-2 cursor-pointer group">
<div class="text-right hidden sm:block">
<p class="text-sm font-bold text-white group-hover:text-primary transition-colors">{{ app.user.prenom }} {{ app.user.nom }}</p>
<p class="text-xs text-gray-400">{{ app.user.role }}</p>
</div>
<div class="size-10 rounded-full bg-cover bg-center border-2 border-surface-dark group-hover:border-primary transition-all" data-alt="Portrait of a male teacher smiling" style="background-image: url('https://lh3.googleusercontent.com/aida-public/AB6AXuA30TiB563k4wMuiOzL4ahQ4IFWqyhrezJLslw-anLa3h6QTuNAG-LO4xj5ByyTu-I7GuTGEOez9rY8GhJOkSrvVSFrkKxCyjDJHqwyMRpi5DPEGMyG3q0qLhd972upzHb5lx5KlLeMklWaBpwk1DCP5fMma93TwF2aURK03CfIkMH1FtpLiwS9lKtlgN1XwQJpBeKx9y-wnxtISkf8DtQSNM31KZxNfPIHULvDxSG_4tWvKewjRzd0B0w2mxUOy_oL1SSyuFfxM0B5');"></div>
</div>
</div>
</header>
<!-- Scrollable Content Area -->
<div class="flex-1 overflow-y-auto p-6 lg:p-10 scroll-smooth">
<div class="max-w-7xl mx-auto space-y-8">
<!-- Welcome & Date -->
<div class="flex flex-col md:flex-row md:items-end justify-between gap-4">
<div>
<h1 class="text-3xl lg:text-4xl font-black tracking-tight text-white mb-2">Bonjour, {{ app.user.prenom }} {{ app.user.nom }} 👋</h1>
<p class="text-gray-400">Voici ce qui se passe dans vos classes aujourd'hui.</p>
</div>
<div class="flex items-center gap-2 text-primary bg-primary/10 px-4 py-2 rounded-full self-start md:self-auto">
<span class="material-symbols-outlined text-sm">calendar_today</span>
<span class="text-sm font-bold">{{ "now"|date("d/m/Y") }}</span>
</div>
</div>
<!-- Main Area -->
<main class="flex-1 overflow-y-auto p-4 md:p-8 scroll-smooth">
<div class="max-w-7xl mx-auto flex flex-col gap-8">
<!-- Header & Actions -->
<div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
<div>
<p class="text-[#9eb7a8] text-sm font-medium mb-1">/ Classes</p>
<h1 class="text-3xl md:text-4xl font-black text-white tracking-tight">Gestion des Classes</h1>
</div>
<button class="flex items-center justify-center gap-2 bg-primary text-black hover:bg-white transition-colors h-12 px-6 rounded-full font-bold shadow-[0_0_20px_rgba(56,224,123,0.3)] hover:shadow-[0_0_30px_rgba(56,224,123,0.5)] active:scale-95 transform duration-200">
<span class="material-symbols-outlined">add_circle</span>
<span>Nouvelle Classe</span>
</button>
</div>
<!-- Filters & Toolbar -->
<div class="bg-surface-dark rounded-2xl p-4 flex flex-col md:flex-row gap-4 items-center border border-[#29382f]">
<!-- Search within page -->
<form method="get" class="flex-1 w-full">
    <div class="relative w-full">
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
            <span class="material-symbols-outlined text-[#9eb7a8] text-[20px]">search</span>
        </div>
        <input 
            name="q" 
            value="{{ app.request.query.get('q') }}" 
            class="block w-full pl-10 pr-4 py-2.5 bg-[#122017] border border-[#29382f] rounded-lg text-white placeholder-[#5a7063] focus:border-primary focus:ring-1 focus:ring-primary text-sm transition-colors" 
            placeholder="Rechercher une classe par nom..." 
            type="text"
        />
    </div>
</form>

<!-- Filters -->
<div class="flex gap-3 w-full md:w-auto">
<div class="relative min-w-[160px] flex-1 md:flex-none">
<form method="get">
    <select name="niveau"
            onchange="this.form.submit()"
            class="appearance-none w-full bg-[#122017] text-white border border-[#29382f]
                   rounded-lg py-2.5 pl-4 pr-10 text-sm cursor-pointer">

        <option value="">Tous les niveaux</option>
        <option value="6" {{ niveau == '6' ? 'selected' : '' }}>6ème</option>
        <option value="5" {{ niveau == '5' ? 'selected' : '' }}>5ème</option>
        <option value="4" {{ niveau == '4' ? 'selected' : '' }}>4ème</option>
        <option value="3" {{ niveau == '3' ? 'selected' : '' }}>3ème</option>
    </select>
</form>


<div class="pointer-events-none absolute inset-y-0 right-0 flex items-center px-3 text-[#9eb7a8]">
<span class="material-symbols-outlined text-sm">expand_more</span>
</div>
</div>
<button class="flex items-center justify-center size-10 rounded-lg border border-[#29382f] text-[#9eb7a8] hover:text-white hover:border-white/20 bg-[#122017] transition-all" title="Vue Liste">
<span class="material-symbols-outlined">format_list_bulleted</span>
</button>
<button class="flex items-center justify-center size-10 rounded-lg border border-primary text-primary bg-primary/10 transition-all" title="Vue Grille">
<span class="material-symbols-outlined">grid_view</span>
</button>
</div>
</div>
<!-- Cards Grid -->
<div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">

{% for relation in relations %}
    {% set classe = relation.classe %}
    {% set matiere = relation.matiere %}

    <div class="bg-surface-dark rounded-xl p-5 border border-white/5
                relative overflow-hidden group transition-all duration-300
                hover:border-primary/40 hover:shadow-lg hover:shadow-primary/10
                flex flex-col h-full">

        <!-- Icône -->
        <div class="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">
            <span class="material-symbols-outlined text-6xl">school</span>
        </div>

        <!-- Header -->
        <div class="flex justify-between items-start mb-4">
            <div class="bg-blue-500/20 text-blue-400 px-3 py-1 rounded-full
                        text-xs font-bold uppercase tracking-wider">
                {{ classe.nom }}
            </div>

            <button class="text-gray-500 hover:text-white transition-colors">
                <span class="material-symbols-outlined">more_horiz</span>
            </button>
        </div>

        <!-- Titre -->
        <h3 class="text-2xl font-bold text-white mb-1">
            {{ matiere.libelle }}
        </h3>

        <!-- Infos -->
        <p class="text-gray-400 text-sm mb-6 flex items-center gap-2">
            <span class="material-symbols-outlined text-sm">school</span>
            Niveau :
            <span class="text-white font-medium">{{ classe.niveau }}</span>
        </p>

        <!-- Footer toujours en bas -->
        <div class="mt-auto flex items-center justify-between">
            <div class="text-xs text-gray-500">
                Année scolaire :
                <span class="text-white font-medium">{{ relation.anneeScolaire }}</span>
            </div>

            <a href="{{ path('enseignant_gestion_classes') }}"
               class="bg-primary text-black font-bold text-sm px-4 py-2
                      rounded-full hover:bg-primary-dark transition-colors">
                Gérer
            </a>
        </div>

    </div>

{% else %}
    <p class="text-gray-400">Aucune classe trouvée.</p>
{% endfor %}



</div>

<!-- Footer / Pagination -->

{% set start = (page - 1) * limit + 1 %}
{% set end = start + relations|length - 1 %}
{% set totalPages = (total / limit)|round(0, 'ceil') %}

<div class="flex items-center justify-between mt-4 pb-8">
    <p class="text-sm text-[#9eb7a8]">
        Affichage de
        <span class="text-white font-bold">{{ start }}-{{ end }}</span>
        sur
        <span class="text-white font-bold">{{ total }}</span>
        classes
    </p>

    <div class="flex gap-2">
        <!-- Précédent -->
        <a href="{{ path('enseignant_gestion_classes', { page: page - 1 }) }}"
           class="px-4 py-2 rounded-lg bg-[#29382f] text-white text-sm font-medium
                  hover:bg-primary hover:text-black transition-colors
                  {{ page == 1 ? 'opacity-50 pointer-events-none' : '' }}">
            Précédent
        </a>

        <!-- Suivant -->
        <a href="{{ path('enseignant_gestion_classes', { page: page + 1 }) }}"
           class="px-4 py-2 rounded-lg bg-[#29382f] text-white text-sm font-medium
                  hover:bg-primary hover:text-black transition-colors
                  {{ page == totalPages ? 'opacity-50 pointer-events-none' : '' }}">
            Suivant
        </a>
    </div>
</div>

</div>
</main>
</div>
</body></html>

