<!DOCTYPE html>

<html class="dark" lang="fr"><head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Enseignant Dashboard - School RMS</title>
<!-- Google Fonts -->
<link href="https://fonts.googleapis.com" rel="preconnect"/>
<link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@400;500;700;900&amp;family=Spline+Sans:wght@300;400;500;600;700&amp;display=swap" rel="stylesheet"/>
<!-- Material Symbols -->
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet"/>
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet"/>
<!-- Tailwind CSS -->
<script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
<!-- Tailwind Configuration -->
<script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#38e07b",
                        "primary-dark": "#2ec268",
                        "background-light": "#f6f8f7",
                        "background-dark": "#111714",
                        "surface-dark": "#1d2621",
                        "surface-light": "#ffffff",
                        "border-dark": "#29382f",
                    },
                    fontFamily: {
                        "display": ["Spline Sans", "Noto Sans", "sans-serif"],
                        "body": ["Noto Sans", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "1rem",
                        "lg": "1.5rem",
                        "xl": "2rem",
                        "2xl": "3rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
<style>
        body {
            font-family: "Spline Sans", "Noto Sans", sans-serif;
        }
        /* Custom scrollbar for dark theme */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #111714; 
        }
        ::-webkit-scrollbar-thumb {
            background: #29382f; 
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #38e07b; 
        }
    </style>
</head>
<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-white h-screen overflow-hidden flex selection:bg-primary selection:text-black">
<!-- Sidebar Navigation -->
<aside class="hidden lg:flex flex-col w-72 h-full border-r border-border-dark dark:border-border-dark bg-background-light dark:bg-background-dark p-6">
<!-- Logo Area -->
<div class="flex items-center gap-3 mb-10 px-2">
<div class="size-10 bg-primary/20 rounded-xl flex items-center justify-center text-primary">
<span class="material-symbols-outlined">school</span>
</div>
<h2 class="text-xl font-bold tracking-tight">School RMS</h2>
</div>
<!-- Navigation Links -->
<nav class="flex-1 flex flex-col gap-2">
<a class="flex items-center gap-4 px-4 py-3 rounded-full bg-primary/10 text-primary transition-all" href="{{path('enseignant_dashboard')}}">
<span class="material-symbols-outlined filled">dashboard</span>
<span class="text-sm font-bold">Dashboard</span>
</a>
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="{{ path('enseignant_gestion_classes') }}">
<span class="material-symbols-outlined">class</span>
<span class="text-sm font-medium">Classes</span>
</a>
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="{{path('enseignant_gestion_etudiants')}}">
<span class="material-symbols-outlined">groups</span>
<span class="text-sm font-medium">Étudiants</span>
</a>
<div class="mt-auto">
<a class="flex items-center gap-4 px-4 py-3 rounded-full text-slate-500 dark:text-gray-400 hover:bg-surface-dark hover:text-white transition-all" href="#">
<span class="material-symbols-outlined">settings</span>
<span class="text-sm font-medium">Paramètres</span>
</a>
</div>
</nav>
</aside>
<!-- Main Content Wrapper -->
<main class="flex-1 flex flex-col h-full overflow-hidden relative">
<!-- Top Navigation Bar -->
<header class="flex items-center justify-between px-6 py-4 border-b border-border-dark dark:bg-background-dark z-20">
<!-- Mobile Menu Toggle (Visible only on small screens) -->
<button class="lg:hidden p-2 text-white">
<span class="material-symbols-outlined">menu</span>
</button>
<!-- Search Bar -->
<div class="hidden md:flex flex-1 max-w-lg">
<label class="flex w-full items-center gap-3 bg-surface-dark rounded-full px-4 py-2.5 border border-transparent focus-within:border-primary/50 transition-all">
<span class="material-symbols-outlined text-gray-400">search</span>
<input class="bg-transparent border-none text-white placeholder-gray-500 focus:ring-0 w-full text-sm" placeholder="Rechercher un élève, une classe..." type="text"/>
</label>
</div>
<!-- Right Actions -->
<div class="flex items-center gap-4 ml-auto">
<button class="relative p-2.5 rounded-full bg-surface-dark hover:bg-white/10 text-white transition-colors">
<span class="material-symbols-outlined text-[20px]">notifications</span>
<span class="absolute top-2 right-2.5 size-2 bg-primary rounded-full"></span>
</button>
<div class="flex items-center gap-3 pl-2 cursor-pointer group">
<div class="text-right hidden sm:block">
<p class="text-sm font-bold text-white group-hover:text-primary transition-colors">{{ app.user.prenom }} {{ app.user.nom }}</p>
<p class="text-xs text-gray-400">{{ app.user.role }}</p>
</div>
<div class="size-10 rounded-full bg-cover bg-center border-2 border-surface-dark group-hover:border-primary transition-all" data-alt="Portrait of a male teacher smiling" style="background-image: url('https://lh3.googleusercontent.com/aida-public/AB6AXuA30TiB563k4wMuiOzL4ahQ4IFWqyhrezJLslw-anLa3h6QTuNAG-LO4xj5ByyTu-I7GuTGEOez9rY8GhJOkSrvVSFrkKxCyjDJHqwyMRpi5DPEGMyG3q0qLhd972upzHb5lx5KlLeMklWaBpwk1DCP5fMma93TwF2aURK03CfIkMH1FtpLiwS9lKtlgN1XwQJpBeKx9y-wnxtISkf8DtQSNM31KZxNfPIHULvDxSG_4tWvKewjRzd0B0w2mxUOy_oL1SSyuFfxM0B5');"></div>
</div>
</div>
</header>
<!-- Scrollable Content Area -->
<div class="flex-1 overflow-y-auto p-6 lg:p-10 scroll-smooth">
<div class="max-w-7xl mx-auto space-y-8">
<!-- Welcome & Date -->
<div class="flex flex-col md:flex-row md:items-end justify-between gap-4">
<div>
<h1 class="text-3xl lg:text-4xl font-black tracking-tight text-white mb-2">Bonjour, {{ app.user.prenom }} {{ app.user.nom }} 👋</h1>
<p class="text-gray-400">Voici ce qui se passe dans vos classes aujourd'hui.</p>
</div>
<div class="flex items-center gap-2 text-primary bg-primary/10 px-4 py-2 rounded-full self-start md:self-auto">
<span class="material-symbols-outlined text-sm">calendar_today</span>
<span class="text-sm font-bold">{{ "now"|date("d/m/Y") }}</span>
</div>
</div>
<!-- Stats Overview -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-4">
<div class="bg-surface-dark p-6 rounded-xl border border-white/5 flex flex-col justify-between h-32 hover:border-primary/30 transition-all cursor-pointer group">
<div class="flex justify-between items-start">
<span class="text-gray-400 text-sm font-medium">Total Étudiants</span>
<span class="material-symbols-outlined text-primary bg-primary/10 p-1.5 rounded-lg group-hover:scale-110 transition-transform">groups</span>
</div>
<p class="text-3xl font-bold text-white">{{ nbEtudiants }}</p>
</div>
<div class="bg-surface-dark p-6 rounded-xl border border-white/5 flex flex-col justify-between h-32 hover:border-primary/30 transition-all cursor-pointer group">
<div class="flex justify-between items-start">
<span class="text-gray-400 text-sm font-medium">Notes en attente</span>
<span class="material-symbols-outlined text-orange-400 bg-orange-400/10 p-1.5 rounded-lg group-hover:scale-110 transition-transform">pending_actions</span>
</div>
<p class="text-3xl font-bold text-white">{{ nbNotesEnAttente }}</p>
</div>
</div>
<!-- Main Layout Grid: Classes/Subjects (Left) & Activity (Right) -->
<div class="grid grid-cols-1 xl:grid-cols-3 gap-8">
<!-- Left Column: Classes & Subjects -->
<div class="xl:col-span-2 space-y-8">
<!-- Classes Assignées -->
<section>
<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {% for affectation in affectations %}
        {% set classe = affectation.classe %}
        {% set matiere = affectation.matiere %}
        {% set nbEtudiantsClasse = nbEtudiantsParClasse[classe.id] ?? 0 %}

        <div class="bg-surface-dark rounded-xl p-5 border border-white/5 relative overflow-hidden group">

            <div class="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">
                <span class="material-symbols-outlined text-6xl">school</span>
            </div>

            <div class="flex justify-between items-start mb-4">
                <div class="bg-blue-500/20 text-blue-400 px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider">
                    {{ classe.nom }}
                </div>
                <button class="text-gray-500 hover:text-white">
                    <span class="material-symbols-outlined">more_horiz</span>
                </button>
            </div>

            <h3 class="text-2xl font-bold text-white mb-1">
                {{ matiere.libelle }}
            </h3>

            <p class="text-gray-400 text-sm mb-6 flex items-center gap-2">
                <span class="material-symbols-outlined text-sm">person</span>
                {{ nbEtudiantsClasse }} Étudiants
            </p>

            <div class="flex items-center justify-between mt-auto">
                <div class="text-xs text-gray-500">
                    Année scolaire :
                    <span class="text-white font-medium">
                        {{ affectation.anneeScolaire }}
                    </span>
                </div>

                <a href="#"
                   class="bg-primary text-black font-bold text-sm px-4 py-2 rounded-full hover:bg-primary-dark transition-colors">
                    Gérer
                </a>
            </div>
        </div>
    {% else %}
        <p class="text-gray-400">Aucune classe assignée.</p>
    {% endfor %}
</div>

</section>
<!-- Matières Enseignées -->
<section>
    <h2 class="text-xl font-bold text-white mb-4">Matières Enseignées</h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        {% for data in matieres %}
            {% set matiere = data.matiere %}
            {% set nbClasses = data.classes|length %}

            <div class="bg-surface-dark p-4 rounded-xl border border-white/5 flex items-center gap-4 hover:bg-white/5 transition-colors cursor-pointer">

                <div class="size-12 rounded-full bg-indigo-500/20 flex items-center justify-center text-indigo-400">
                    <span class="material-symbols-outlined">menu_book</span>
                </div>

                <div>
                    <h4 class="font-bold text-white">
                        {{ matiere.libelle ?? matiere.nom ?? 'Matière' }}
                    </h4>

                    <p class="text-xs text-gray-400">
                        {{ nbClasses }} Classe{{ nbClasses > 1 ? 's' : '' }}
                        • —
                        H / semaine
                    </p>
                </div>

                <span class="material-symbols-outlined text-gray-600 ml-auto">
                    chevron_right
                </span>
            </div>
        {% else %}
            <p class="text-gray-400">Aucune matière assignée.</p>
        {% endfor %}
    </div>
</section>
</div>
<!-- Right Column: Recent Activity -->
<div class="space-y-6">
    {% for note in dernieresNotes %}
        <div class="flex gap-4 group">

            {# Avatar simple avec initiales #}
            <div class="size-10 rounded-full bg-primary/20 flex items-center justify-center text-primary font-bold text-xs shrink-0">
                {{ note.eleve.prenom|first }}{{ note.eleve.nom|first }}
            </div>

            <div class="flex-1 min-w-0">
                <div class="flex justify-between items-start">
                    <p class="text-sm font-bold text-white truncate">
                        {{ note.eleve.prenom }} {{ note.eleve.nom }}
                    </p>

                    {# Couleur selon la note #}
                    {% set couleur =
                        note.valeur >= 16 ? 'green' :
                        note.valeur >= 10 ? 'yellow' :
                        'red'
                    %}

                    <span class="text-xs font-bold text-{{ couleur }}-400 bg-{{ couleur }}-400/10 px-2 py-0.5 rounded">
                        {{ note.valeur }}/20
                    </span>
                </div>

                <p class="text-xs text-gray-400 truncate">
                    {{ note.type }} — {{ note.matiere.libelle ?? note.matiere.nom }}
                </p>

                <p class="text-[10px] text-gray-600 mt-1">
                    {{ note.dateNote|time_ago }}
                </p>
            </div>
        </div>

        {% if not loop.last %}
            <hr class="border-white/5"/>
        {% endif %}
    {% else %}
        <p class="text-gray-400 text-sm">
            Aucune note saisie récemment.
        </p>
    {% endfor %}

    <button class="w-full py-2.5 rounded-full border border-white/10 text-gray-400 text-sm font-medium hover:bg-white/5 hover:text-white transition-colors mt-2">
        Voir tout l'historique
    </button>
</div>

</main>
</body></html>